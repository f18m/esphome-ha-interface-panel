# File: lvgl_ui_design.yaml
# Purpose: Declares the main LVGL page layout, tabs, and included UI
# components for the panel.
#

lvgl:
  rotation: ${display_rotation}
  pages:
    - id: page_main
      bg_color: 0x0F172A
      widgets:
        - label:
            id: lbl_time
            x: 5
            y: 8
            width: 200
            height: 30
            text: "10/03/2026 12:00"
            text_color: ${label_title_color}
            text_font: visuelt_20

        - label:
            id: lbl_wifi
            x: 415 # screen width is 480
            y: 0
            width: 50
            height: 35
            text: "$mdi_wifi"
            text_font: mdi_icons_20
            text_align: RIGHT
            text_color: ${icon_off_color}

        - tabview:
            id: tabview_main
            x: 0
            y: 35
            width: 480
            height: 285
            bg_color: $bg_color
            scrollable: false
            tab_style:
              #border_color: $icon_on_color
              #border_width: 6
              # height must be enough to be confortable to press:
              height: 45
              bg_color: white
              items:
                bg_color: $border_color
                text_color: $label_tabs_color
                text_font: $icon_font
            tabs:

              # each of this tab has 
              # FULL WIDTH AVAILABLE: 480px
              # HEIGHT AVAILABLE: 240px

              - name: "${mdi_thermometer_water}"
                scrollable: false
                widgets: !include lvgl_tab_rooms_${num_rooms}.yaml

              - name: "${mdi_thermometer_chevron_up}"
                scrollable: false
                widgets: !include lvgl_tab_heating_set_temp.yaml

              - name: "${mdi_radiator}"
                scrollable: false
                widgets: !include lvgl_tab_heating_details.yaml

              - name: "${mdi_window_shutter}"
                scrollable: false
                widgets: !include lvgl_tab_covers.yaml

              - name: "${mdi_cog}"
                scrollable: false
                widgets: !include lvgl_tab_scenes.yaml

              - name: "${mdi_information}"
                scrollable: false
                widgets: !include lvgl_tab_info.yaml
